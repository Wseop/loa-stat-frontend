import { Board } from '@/components/commons/area';
import { CharactersSkillProps } from './interfaces/characters.interface';

export default function CharactersSkillBoard(props: CharactersSkillProps) {
  return <Board>SKILL</Board>;
}
