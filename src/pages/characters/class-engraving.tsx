import { Board } from '@/components/commons/area';
import { CharactersClassEngravingProps } from './interfaces/characters.interface';

export default function CharactersClassEngravingBoard(
  props: CharactersClassEngravingProps
) {
  return <Board>CLASS_ENGRAVING</Board>;
}
