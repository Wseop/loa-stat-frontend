import { Board } from '@/components/commons/area';
import { CharactersServerProps } from './interfaces/characters.interface';

export default function CharactersServerBoard(props: CharactersServerProps) {
  return <Board>SERVER</Board>;
}
